mutation($emailOrUsername: String!, $password: String!) {
  logInWithPassword(
    emailOrUsername: $emailOrUsername,
    password: $password
  ) {
    token
    user {
      id
      username
    }
  }
}
